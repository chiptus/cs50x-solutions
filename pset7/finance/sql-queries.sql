CREATE TABLE transactions ( id INTEGER PRIMARY KEY, user_id INTEGER NOT NULL, symbol TEXT NOT NULL, price NUMERIC NOT NULL, timestamp DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, 'type' INTEGER NOT NULL, 'shares' INTEGER NOT NULL DEFAULT 0, FOREIGN KEY (user_id) REFERENCES users(id) )